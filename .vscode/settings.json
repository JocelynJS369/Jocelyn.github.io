{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "MkDocs Serve",
      "type": "python",
      "request": "launch",
      "program": "mkdocs",
      "args": ["serve", "--dev-addr=localhost:8000"],
      "cwd": "${workspaceFolder}"
    }
  ]
}